{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core defaults plus native folder picker, logging, the auto-updater and app relaunch (to apply updates). File reads happen in app-defined Rust commands (no broad fs scope needed).",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:default",
    "log:default",
    "updater:default",
    "process:default"
  ]
}
