{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core defaults plus native folder picker, logging and the auto-updater. File reads happen in app-defined Rust commands (no broad fs scope needed).",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:default",
    "log:default",
    "updater:default"
  ]
}
